MediMix Demo Share Package

打开方式：
1. 如果你拿到的是 `MediMix-demo-share.html`，直接双击打开即可。
2. 如果你拿到的是 `MediMix-demo-share.zip`，先解压，再打开其中的 `MediMix-demo-share.html`。

说明：
- 这是单文件分享版，适合通过微信、邮件、AirDrop 发送。
- 页面包含可点击切换和自动轮播演示。
- 建议使用 Chrome 或 Safari 打开。
